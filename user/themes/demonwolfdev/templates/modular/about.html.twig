<!-- About the community -->
<div class="about" id="about">
    <div class="container">
        <div class="header">
            <p>{{ page.header.about.header }}</p>
        </div>
        <div class="text">
            {% for section in page.header.about.text %}
                <p>{{ section | textilize }}</p>
            {% endfor %}
        </div>
    </div>
</div>